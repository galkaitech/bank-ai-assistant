# BankMind — AI Banking Operations Copilot
# Python 3.11+ required
#
# Dependencies are pinned to minor versions (x.y.*) to allow patch-level
# security updates without introducing breaking changes. Lock to exact
# versions (x.y.z) in production via `pip freeze > requirements.lock`.

# ---------------------------------------------------------------------------
# LLM & Orchestration
# ---------------------------------------------------------------------------
openai==1.35.14
langchain==0.2.14
langchain-openai==0.1.22
langchain-community==0.2.12
langchain-core==0.2.38
langchain-text-splitters==0.2.4
langgraph==0.2.4

# ---------------------------------------------------------------------------
# Vector Store
# ---------------------------------------------------------------------------
chromadb==0.5.5
# pinecone-client==3.2.2  # Uncomment for Pinecone production deployment

# ---------------------------------------------------------------------------
# API Framework
# ---------------------------------------------------------------------------
fastapi==0.111.1
uvicorn[standard]==0.30.3
python-multipart==0.0.9     # Required for FastAPI file upload endpoints

# ---------------------------------------------------------------------------
# Configuration & Environment
# ---------------------------------------------------------------------------
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# Data Processing
# ---------------------------------------------------------------------------
pandas==2.2.2
numpy==1.26.4

# ---------------------------------------------------------------------------
# Document Processing
# ---------------------------------------------------------------------------
pypdf==4.3.1                # PDF loading for policy documents
# python-docx==1.1.0        # Uncomment for DOCX support
# unstructured==0.15.0      # Uncomment for advanced document parsing

# ---------------------------------------------------------------------------
# Optional Frontend
# ---------------------------------------------------------------------------
streamlit==1.37.1           # Chat UI for non-technical users

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
httpx==0.27.0               # Async HTTP client (used by OpenAI SDK)
tenacity==8.5.0             # Retry logic for LLM API calls
jinja2==3.1.4               # Report template rendering

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0

# ---------------------------------------------------------------------------
# Code Quality
# ---------------------------------------------------------------------------
flake8==7.1.0
black==24.8.0               # Code formatter (optional but recommended)
isort==5.13.2               # Import sorter
mypy==1.11.1               # Static type checking
